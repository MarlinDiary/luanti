# 真实课程服生存 SDK 回归（2026-09-09）

## 访问与范围

通过 Chrome 扩展进入已登录课程门户，启动原有世界；核对现行连接信息后，使用官方客户端 `--password-file` 参数读取继承的匿名内存管道，走正常服务器身份认证。密码未写入文件、配置或命令行参数值。

实际连接 `play.minecraft.elliottwen.info:30000`，角色 `cni586`。画面服务器公告确认 Luanti 5.16.1 / VoxeLibre；课程客户端 0.8.0（引擎 5.17.0）保持不变，SDK 从 0.11.0 修至 0.11.1。

未重置世界、安装服务端模组、修改地图/课程配置、授予资源或更改时间天气。测试前门户为闲置停止；测试后确认零在线并停止服务器，门户确认世界保留。计时显示 44m → 45m；普通世界时间在运行中继续推进。

## 同输入故障与修复

在坐标 `[-4.8400, 16.5, -3.1130]`、HP 20、呼吸 10、饥饿 20、干燥陆地上调用 `recover_air()`：

- 0.11.0：`blocked / air_route_unavailable`，错误尝试寻找水面。
- 0.11.1：`success / already_breathing`，不移动。

修改分支在 `sdk/src/luanti_course/skills.py::regain_air`。必须先确认身体占用空间已观察、干燥、无碰撞；不依据“呼吸满”就跳过水下撤离。干燥但低呼吸时原地有界等待实际回升，期间保留控制权/死亡/取消检查。再次入水会转回原水面路线。

## 本轮真实服 5 项检查

1. 新观察字段：饥饿 20、实体观察可用、36 个持有栏位武器数据。
2. 选择已有装备：选中已有木镐；背包数量未变化。
3. 自动食物选择：满饥饿返回 `already_full`，未消耗食物。
4. 干燥陆地恢复呼吸：修复前失败、修复后原地成功。
5. 无接触伤害时脱险：原地成功，无不必要移动。

额外检查：默认快速反应层在安静环境下不做多余动作；`stop()` 暂停反应层并清空输入/转向；退出恢复观察模式。两轮真实服角色位置、HP 20、背包均未变化。测试音频后端关闭、窗口未聚焦，鼠标审计无抓取/归中；两个测试客户端进程均正常退出（exit 0）。

**这不等于实战验证。** 本轮周围没有敌对实体，角色不饿、不在水中或火中。因此实际战斗/追击/避敌、低血撤离、真实食物消耗、水险/火险触发仍未完成真实服验收。之前本地 17 项生存场景和远程 17 项旧基础技能测试不挪作这些项目的证据。

## 修复回归

- 新增 7 项回归测试：原代码出现 1 failure + 1 error，修复后 7/7。
- 完整单元测试：297/297。
- 安装 wheel 后在持续运行的隔离本地客户端重测水下恢复呼吸：通过，呼吸 3 → 10。
- 首次本地水下重测失败：暂停 833 秒的服务器恢复时记录大时间步，场景初始化期间已经溺亡、控制权被释放，技能尚未执行。保留该失败；待现有进程恢复正常计时后，同一场景重测通过。没有为了通过而放宽死亡/控制权保护。这个长时间进程暂停后的计时问题仍是测试设施的已知限制；本轮结束保持原进程低帧率、停输入待命，不再 SIGSTOP。
- 初次完整测试副本缺失 engine 文件，补齐未修改的 engine 后 297 项通过；系统 Python 首次误打包 UNKNOWN wheel，换用项目既有打包环境成功。失败记录保留，UNKNOWN wheel 不发布。

## 证据与回退

- 原始真实服失败：`build/evidence/survival-v0.16/remote-approved/`。
- 修复与回归：`build/evidence/air-recovery-v0.17/`，包括 `remote-patched/`、`baseline-new-tests.json`、`patched-new-tests.json`、`patched-unit-complete.json`、`local-air-warmed.json`。
- 发布包：`dist/luanti_course-0.11.1-py3-none-any.whl`；原 0.11.0 wheel 保留。
- `air-recovery.patch`、`verification.json`、`rollback.py` 位于上述修复证据目录。回退只恢复本次源文件/文档/测试和新增 wheel，不触碰游戏世界、服务器或用户配置。先回退本次，再使用上一版本的回退脚本。
- Windows 原生构建与实机验收仍未完成。
